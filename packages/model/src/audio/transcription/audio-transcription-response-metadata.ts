import { AbstractResponseMetadata } from "../../model/abstract-response-metadata.js";

/**
 * Metadata associated with an audio transcription response.
 */
export class AudioTranscriptionResponseMetadata extends AbstractResponseMetadata {}
