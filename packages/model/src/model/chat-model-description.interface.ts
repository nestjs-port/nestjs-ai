import type { ModelDescription } from "./model-description.interface";

/**
 * Marker interface, to be used to store info on the model such as the current context
 * length.
 */
export type ChatModelDescription = ModelDescription;
