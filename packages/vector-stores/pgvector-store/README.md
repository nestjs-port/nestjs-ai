# `@nestjs-ai/vector-store-pgvector`

Scaffold package for the Spring AI `PgVectorStore` TypeScript port.

Planned responsibilities:

- JSDbC-based database access
- PostgreSQL `pgvector` persistence
- similarity search with distance operators
- metadata filtering
- table and index initialization
